host: 'kong:8001'
headers: []
apis:
  - name: account.local # unique api name
    ensure: "present" # Set to "removed" to have Kongfig ensure the API is removed. Default is prese$
    plugins:
      - name: cors
        attributes:
          config:
            preflight_continue: false
            credentials: false
    attributes:
      request_host: account.sandbox.koodaacraft.org
      strip_request_path: true
      preserve_host: false
      upstream_url: http://account:8080 # (required)
  - name: operator.local # unique api name
    ensure: "present" # Set to "removed" to have Kongfig ensure the API is removed. Default is prese$
    plugins:
      - name: cors
        attributes:
          config:
            preflight_continue: false
            credentials: false
    attributes:
      request_host: operator-mgmt.sandbox.koodaacraft.org
      strip_request_path: true
      preserve_host: false
      upstream_url: http://operator_components:5000 # (required)
  - name: service_components.local # unique api name
    ensure: "present" # Set to "removed" to have Kongfig ensure the API is removed. Default is prese$
    plugins:
      - name: cors
        attributes:
          config:
            preflight_continue: false
            credentials: false
    attributes:
      request_host: service-mgmt.sandbox.koodaacraft.org
      strip_request_path: true
      preserve_host: false
      upstream_url: http://service_components:7000 # (required)
  - name: service_mockup.local # unique api name
    ensure: "present" # Set to "removed" to have Kongfig ensure the API is removed. Default is prese$
    plugins:
      - name: cors
        attributes:
          config:
            preflight_continue: false
            credentials: false
    attributes:
      request_host: service-mockup.sandbox.koodaacraft.org
      strip_request_path: true
      preserve_host: false
      upstream_url: http://service_mockup:2000 # (required)

