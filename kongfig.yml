host: 'kong:8001'
headers: []
apis:
  - name: account.local # unique api name
    ensure: "present" # Set to "removed" to have Kongfig ensure the API is removed. Default is prese$
    plugins:
      - name: cors
        attributes:
          config:
            preflight_continue: false
            credentials: false
    attributes:
      request_host: account.sandbox.koodaacraft.org
      strip_request_path: true
      preserve_host: false
      upstream_url: http://account:8080 # (required)
  - name: operator.local # unique api name
    ensure: "present" # Set to "removed" to have Kongfig ensure the API is removed. Default is prese$
    plugins:
      - name: cors
        attributes:
          config:
            preflight_continue: false
            credentials: false
    attributes:
      request_host: operator-mgmt.sandbox.koodaacraft.org
      strip_request_path: true
      preserve_host: false
      upstream_url: http://operator_components:5000 # (required)

  - name: service_component_source.local # unique api name
    ensure: "present" # Set to "removed" to have Kongfig ensure the API is removed. Default is prese$
    plugins:
      - name: cors
        attributes:
          config:
            preflight_continue: false
            credentials: false
    attributes:
      request_host: service-source.sandbox.koodaacraft.org
      strip_request_path: true
      preserve_host: false
      upstream_url: http://service_component_source:7000 # (required)

  - name: service_component_sink.local # unique api name
    ensure: "present" # Set to "removed" to have Kongfig ensure the API is removed. Default is prese$
    plugins:
      - name: cors
        attributes:
          config:
            preflight_continue: false
            credentials: false
    attributes:
      request_host: service-sink.sandbox.koodaacraft.org
      strip_request_path: true
      preserve_host: false
      upstream_url: http://service_component_sink:7000 # (required)


  - name: service_mockup_source.local # unique api name
    ensure: "present" # Set to "removed" to have Kongfig ensure the API is removed. Default is prese$
    plugins:
      - name: cors
        attributes:
          config:
            preflight_continue: false
            credentials: false
    attributes:
      request_host: service-mockup-source.sandbox.koodaacraft.org
      strip_request_path: true
      preserve_host: false
      upstream_url: http://service_mockup_source:2001 # (required)


  - name: service_mockup_sink.local # unique api name
    ensure: "present" # Set to "removed" to have Kongfig ensure the API is removed. Default is prese$
    plugins:
      - name: cors
        attributes:
          config:
            preflight_continue: false
            credentials: false
    attributes:
      request_host: service-mockup-sink.sandbox.koodaacraft.org
      strip_request_path: true
      preserve_host: false
      upstream_url: http://service_mockup_sink:2002 # (required)

